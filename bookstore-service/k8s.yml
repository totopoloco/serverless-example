apiVersion: apps/v1
kind: Deployment
metadata:
  name: "bookstore-service"
spec:
  selector:
    matchLabels:
      app: "bookstore-service"
  template:
    metadata:
      labels:
        app: "bookstore-service"
    spec:
      containers:
        - name: "bookstore-service"
          image: "gcr.io/bookstore-service/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "bookstore-service"
spec:
  selector:
    app: "bookstore-service"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080